.DS_Store
__pycache__/
*.pyc
create_deck.py
BloodsRUs_Proposal.pdf
.vercel
.claude/

# Scaffolding artifacts from Track A tagging refactor (one-time)
content.json.before_apply
extracted_values.json
drift_report.json
tag_report.json
# Truth backup (gitignore so it doesn't drift)
index.html.backup

# Build output (generated by build.js on every deploy)
public/
dist/
# index.html is a generated build output (build.js writes it into public/ and a
# root copy for local preview). Not committed so Vercel must run the build and
# scan api/ for Serverless Functions instead of serving a prebuilt static site.
index.html
